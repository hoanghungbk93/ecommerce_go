version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgres://admin:1234@host.docker.internal:5432/ecommerce?sslmode=disable
      JWT_SECRET: your-super-secret-jwt-key-change-in-production
      PORT: 8080
      VNPAY_TMN_CODE: your-vnpay-tmn-code
      VNPAY_HASH_KEY: your-vnpay-hash-key
      VNPAY_URL: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
      VNPAY_RETURN_URL: http://localhost:3001/payment/return
      ENVIRONMENT: production
    ports:
      - "8080:8080"
    networks:
      - ecommerce-network
      - webhook-service_webhook-network
    labels:
      - "logging=promtail"
      - "service=backend"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      REACT_APP_API_URL: http://localhost:8080/api/v1
    ports:
      - "3001:80"
    depends_on:
      - backend
    networks:
      - ecommerce-network

networks:
  ecommerce-network:
    driver: bridge
  webhook-service_webhook-network:
    external: true
